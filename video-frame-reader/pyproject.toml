[project]
name = "video-frame-reader"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.9.0",
    "Pillow>=10.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "numpy>=1.26.0",
]
